/** Plain ASCII domain — use in descriptions, text content, anywhere that could be auto-linked */
export const SITE_DOMAIN = "dalat.app";

/** Full site URL */
export const SITE_URL = `https://${SITE_DOMAIN}`;

/** Branded display name — use in titles, siteName, UI branding only */
export const SITE_NAME = "ĐàLạt.app";

/** Default OG image */
export const DEFAULT_OG_IMAGE = `${SITE_URL}/og-image.png?v=2`;
